services:

  database:
    image: mariadb:10.6.20
    container_name: oss-mariadb-2025
    ports:
      - "3307:3306"
    env_file:
      - .env
    volumes:
      - dbliferay2025:/var/lib/mysql
      - ./resources/dump-liferay.sql:/docker-entrypoint-initdb.d/dump.sql
    restart: always
    command: [ 'mysqld', '--character-set-server=utf8', '--collation-server=utf8_general_ci' ]
    networks:
      - liferay

  mariadb-keycloak:
    image: mariadb:10.6.20
    container_name: keycloak-mariadb-2025
    ports:
      - "3308:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=rootpassword
      - MYSQL_DATABASE=keycloak
      - MYSQL_USER=keycloak
      - MYSQL_PASSWORD=password
      - HOSTNAME=mariadb-keycloak
    volumes:
      - dbkeycloak2025:/var/lib/mysql
      - ./resources/dump-keycloak.sql:/docker-entrypoint-initdb.d/dump.sql
    restart: always
    command: [ 'mysqld', '--character-set-server=utf8', '--collation-server=utf8_general_ci' ]
    networks:
      - liferay

  liferay:
    container_name: oss-liferay-2025
    build: liferay
    ports:
      #      - 80:8080
      - 8081:8080
      - 8000:8000
    volumes:
      - ./liferay/data:/opt/liferay/data/document_library
      - ./liferay/mount/files:/mnt/liferay/files
      - ./liferay/geoip:/opt/liferay/geoip
      - ./liferay/lmcrypt:/opt/liferay/lmcrypt
      - ./liferay/patching:/mnt/liferay/patching
      - ${LIFERAY_BUNDLE_DIR}/deploy:/opt/liferay/deploy
      - ${LIFERAY_BUNDLE_DIR}/osgi/modules:/opt/liferay/osgi/modules
    restart: always
    networks:
      - liferay

  elasticsearch:
    container_name: oss-elastic-2025
    build: elasticsearch
    environment:
      - cluster.name=oss-cluster
      - bootstrap.memory_lock=true
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms2048m -Xmx2048m"
    ports:
      - 9200:9200
    volumes:
      - esdata2025:/usr/share/elasticsearch/data
    ulimits:
      memlock:
        soft: -1
        hard: -1
    networks:
      - liferay

  keycloak:
    image: quay.io/keycloak/keycloak:24.0.1
    ports:
      - 8080:8080
      - 8787:8787
    entrypoint: [ "/opt/keycloak/bin/kc.sh", "start-dev", "--import-realm", "--log-level=INFO" ]
    volumes:
      - ./keycloak/deployments:/opt/keycloak/providers
      - ./resources/realm-export.json:/opt/keycloak/data/import/realm-export.json
      - ./resources/keycloak.conf:/opt/keycloak/conf/keycloak.conf
    restart: always
    networks:
      - liferay
    environment:
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=Pa55w0rd
      - DEBUG=true
      - DEBUG_PORT=*:8787
      - KC_HTTP_RELATIVE_PATH=/auth
    depends_on:
      - mariadb-keycloak

volumes:
  esdata2025:
    driver: local
  dbliferay2025:
    driver: local
  dbkeycloak2025:
    driver: local

networks:
  liferay:
